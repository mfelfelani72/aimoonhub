version: '3.8'
services:

# NGINX Service
  webserver:
    image: nginx:alpine
    container_name: webserver-aimoonhub
    restart: always
    tty: true
    ports:
      - "80:20080"
      - "443:20443"
    volumes:
      - "./:/var/www"
      - "./docker-files/nginx/conf.d/:/etc/nginx/conf.d/"
 
